    import XCTest
    @testable import ManySheets

    final class ManySheetsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ManySheets().text, "Hello, World!")
        }
    }
